# Caesar Cipher (Python CLI)

Simple command-line Caesar cipher that can encode and decode messages.

## Features
- Encode / Decode modes
- Supports uppercase and lowercase letters
- Preserves spaces, punctuation, and numbers
- Shift normalization (wrap-around using modulo)

## Usage
Run the script:

```bash
python main.py
